/**
 * function should return index of first instance of the searching element, do not use built-in methods (find, findIndex)
 * @return index of element or undefined
 */
export function linearSearch<T = any>(
  arr: T[],
  searchElement: T
): number | undefined {
  throw new Error('Not implemented jet');
}
